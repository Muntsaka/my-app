
import React from 'react';
import {View} from 'react-native';

const list = () => {
    return array.map((tales) => {
        return (
          <View key={element.name} style={{margin: 15, border: 15}}></View>
           <View key={element.author} style={{margin: 15, border: 15}></View>

    return <View>{list()}</View>;